<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/style.css">
    <title>Mongoose Store</title>
</head>
<body>
    <img id="img" src=></img>
    <a href="/new">Add a Product</a>
    <ul>
        <% for(let i = 0; i < products.length; i++) { %>
        <li id="li">
            <a href="/<%=i%>"<%=products[i].name %>><img src='<%=products[i].img%>'/></a>
            <form id="form" action="/<%= products._id %>?_method=DELETE" method="POST" >
            <input type="submit" value="DELETE" >
            </form>
            <a href="/<%=i %>/edit">Edit</a>
        </li>
        <% } %>
    </ul>
</body>
</html>